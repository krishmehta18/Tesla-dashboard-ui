{
  "name": "tesla-dashboard-ui",
  "version": "1.0.0",
  "main": "scripts.js",
  "scripts": {
    "start": "serve -s build -l 3000 --listen 0.0.0.0",
    "build": "echo Building Tesla dashboard... && mkdir -p build && echo '<h1>Tesla UI Build</h1>' > build/index.html"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
